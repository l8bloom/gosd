name: "Test PRs"
on:
  workflow_dispatch:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
jobs:
  ubuntu-build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone
        id: checkout
        uses: actions/checkout@v6
      - name: Build
        id: cmake_build
        run: |
          echo "Hello world2."
