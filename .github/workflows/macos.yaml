name: "macOS"
on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/**.yaml'
      - 'pkg/gosd/**.go'
      - 'go.mod'
      - 'go.sum'
jobs:
  macos:
    uses: ./.github/workflows/setup.yaml
    with:
      os: unix
      runner_type: macos-latest
